export default defineAppConfig({
  ui: {
    colors: {
      primary: 'blue',
      secondary: 'purple',
      neutral: 'zinc'
    },
    button: {
      slots: {
        base: 'cursor-pointer'
      }
    }
  }
})
